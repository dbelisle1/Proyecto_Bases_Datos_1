@model Proyecto_Inmuebles.Models.ReportesViewModel
@{
    ViewData["Title"] = "Contraofertas efectivas por agente";
    var rows = Model?.Reporte7 ?? new List<Proyecto_Inmuebles.Models.ContraofertasPorAgente>();
}

<h2>@ViewData["Title"]</h2>

<div class="mt-3" style="max-width:800px;">
    <table class="table table-striped table-bordered table-sm">
        <thead>
            <tr class="text-center">
                <th>Código</th>
                <th>Contraofertas</th>
                <th>Aceptadas</th>
                <th>% Aceptadas</th>
            </tr>
        </thead>
        <tbody class="text-center">
            @if (!rows.Any())
            {
                <tr><td colspan="4" class="text-muted">Sin datos</td></tr>
            }
            else
            {
                foreach (var item in rows)
                {
                    <tr>
                        <td>@item.Codigo</td>
                        <td>@item.Contraofertas</td>
                        <td>@item.Aceptadas</td>
                        <td>@item.PctAceptadas</td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>

<a asp-controller="Reportes" asp-action="Index" class="btn btn-outline-info">@("<- Regresar")</a>
